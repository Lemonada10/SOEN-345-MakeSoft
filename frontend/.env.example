# Optional: API base URL (including /api). Used in production.
# Leave unset for local dev (app uses http://localhost:8080/api via proxy).
# On Vercel, set this in Project Settings → Environment Variables.
# REACT_APP_API_BASE=https://your-backend-api.com/api
